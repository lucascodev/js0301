const page = document.querySelector("#schedules-services");

if (page) {
  const checkBox = document.querySelectorAll(".options input") as NodeList;
  const preco = document.querySelectorAll("tr .price") as NodeList;
  const total = document.querySelectorAll("footer .total") as NodeList;
  
  checkBox[0].addEventListener("click", () => {});
}
